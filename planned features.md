# Client Features
## Client.Log level
Need to actually implement controlling the log level with this setting.
## Network.MaxReconnectAttempts
Need to actually implement the reconnect attempts setting.
## Client Version Checking
Need to actually implement checking the version with the server for compatability checks.

# Server Features
## Security.EnableAuthentication
Need to actually implement authentication.
## Security.AllowedIPs
Need to actually implement IP filtering.
## Performance.HeartbeatInterval
Need to actually implement heartbeat functionality.